{
  "name": "sample-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://siva432/resume",
  "dependencies": {

  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}